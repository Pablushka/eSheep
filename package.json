{
  "name": "web-desktop-pet-demo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.extension.config.ts && vite build --config vite.popup.config.ts",
    "preview": "vite preview",
    "icons": "node scripts/extract-icon.mjs",
    "zip": "bash scripts/package.sh"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "pngjs": "^7.0.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}