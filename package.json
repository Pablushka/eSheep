{
  "name": "web-desktop-pet-demo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.extension.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}